const blocks = (state = {}, { type, payload }) => {
  switch (type) {
    // TODO INIT for hydrating blocks from DB
    // case 'INIT': {
    // }
    default:
      return state;
  }
};

export default blocks;
